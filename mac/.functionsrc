function search() {
	grep -rin . -e $1
}

function scuttle() {
	docker kill $(docker ps -a -q)
	docker rm $(docker ps -a -q)
}

function sink() {
	docker rmi $(docker images -q)
}

function gitt() {
	git add -A
	git commit -m $1
	git push
}
